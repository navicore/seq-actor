# Factorial Example
#
# Classic recursive factorial demonstrating:
# - Multi-clause function definitions
# - Pattern matching on values
# - Recursion

: factorial
  | (0) -> 1
  | (n) -> n * factorial(n - 1)
;

: main () ->
  let result = factorial(10)
  io:println(int:to_string(result))
;
